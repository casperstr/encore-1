diff --git a/modules/standard/String.enc b/modules/standard/String.enc
index 4bdc46cb..b248f87e 100644
--- a/modules/standard/String.enc
+++ b/modules/standard/String.enc
@@ -76,8 +76,8 @@ end
 -- [ ] Write proper docs
 -- Be aware of issue #204
 read class String : Id + Eq[String](compare(),length,data)
-  val data : EMBED char* END
-  val length : int
+  val data : EMBED char* END = s
+  val length : int =  EMBED (int); strlen(#{s}); END
   def init(s:EMBED char* END) : unit
     this.data = s
     this.length = EMBED (int); strlen(#{s}); END
diff --git a/src/parser/Parser/Parser.hs b/src/parser/Parser/Parser.hs
index 0aa2ccd2..a7a4b81a 100644
--- a/src/parser/Parser/Parser.hs
+++ b/src/parser/Parser/Parser.hs
@@ -772,10 +772,13 @@ fieldDecl = do fmeta <- meta <$> getPosition
                fname <- Name <$> identifier
                colon
                ftype <- typ
+               withLinebreaks $ reservedOp "="
+               fexpr <- expression
                return Field{fmeta
                            ,fmut
                            ,fname
-                           ,ftype}
+                           ,ftype
+                         }
 
 paramDecl :: EncParser ParamDecl
 paramDecl = do
